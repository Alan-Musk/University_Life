## 第一章课后习题和问题
### R1:
- 没有什么不同
- PC,智能手机,Web服务器
- 是,是一种端系统

### R2:
- 根据维基百科的描述，外交礼仪通常被定义为一组国际礼仪规则。这些既定且历史悠久的规则使得国家和人民之间更容易地生活和工作。礼仪的一部分始终是对所有在场人员的等级地位的承认。礼仪规则基于礼貌原则。

### R3:
- 标准对于协议很重要,这样人们就可以创建互连的网格系统和产品

### R4:
- 住宅接入:
    1. DSL(Digital Subscriber Line)
    2. 电缆
    3. FTTH(Fiber To The Home)光纤到户
    4. 拨号
    5. 卫星
- 公司接入:
    1. 以太网
    2. 局域网
- 广域无线接入:
    1. 3G和4G
    2. LTE(Long-Term Evolution)

### R5:
- 共享的
- 不可能
- 在下行信道中,因为分组由同一个源发出,但是被不同的端系统(PC,etc)接受

### R6:
- 在大多数美国城市,现在可能有:DSL,FTTP

### R7:
- 用户通常以100Mbps或1Gbps速率接入以太网交换机,而服务器可能具有1Gbps甚至10Gbps的接入速率

### R8:
- 双绞铜线,也可以是光纤链路

### R9:
- 拨号调制解调器:56kbps 专用的
- DSL:
    - 下行:24Mbps 上行:2.5Mbps 
    - 专用的
- FTTH:平均下行20Mbps 专用的
- HFC:下行:42.8Mbps 上行:30.7Mbps 共享的

### R10:
- 目前最流行两种无线因特网接入技术
    1. Wifi(802.11) 在无线的LAN(局域网),无线用户转发/接受 数据包 向/来自 在几十米的半径范围内的一个基站(即无线接入点).基站通常连接到有线互联网从而将无线用户接入有线网络
    2. 3G还有4G是广域无线网络,在他们的系统中,数据包被传播在与被用来电话通信一致的无线基础设施中,基站因此被电信提供商管理.这种方式给用户提供在基站附近数十千米的无线入口

### R11:
- L/R1+L/R2

### R12:
- 电路交换会预先分配传输链路的使用,使链接稳定,适合电话通信 
- TDM(Time-Division Multiplexing)比FDM(Frequency-Division Multiplexing)速率更快,因为FDM是连续地得到部分带宽.TDM是周期性地得到所有带宽

### R13:
- 2位用户
- 因为每人的需求是1Mbps且为两人,故没有排队的情况存在.因为三人的网络理论需求是3Mbps,该线路只有2Mbps
- 20%
- 0.008.因为队列当所有用户在传输分组时才会增长,所以队列增长的时间比率也是0.008
### R14:
- 如果两个级别相同的ISP不互相对等,那么当他们发送流量给对方时,他们必须通过提供商ISP发送流量,必须支付运输流量的费用.通过直接互相对等,两个ISP可以减少他们对上级ISP的付款.
- IXP(Internet Exchange Points)是一个会议点,多个ISP可以连接和/或对等到一起.IXP通过向连接 IXP 的每个 ISP 收取相对较小的费用来赚取利润，这可能取决于发送到 IXP 或从 IXP 接收的流量。
### R15:
- 谷歌的私人网络将他所有的数据中心连接在一起.谷歌数据中心的数据流通过他的私人网络而不是公共网络,他们的许多数据中心坐落在或者靠近低层级的IPS.因此,当谷歌传输内容给一个用户,它能绕过高层级的IPS.
- 首先,自从它使用中间层的ISP,内容提供商可以更好控制用户的体验.第二,可以通过向ISP发送更少数据流来节省支付费用.第三,如果ISP决定向高利润的内容提供商收取更多的费用,他们可以避免额外的支出
### R16:
- 固定:节点处理时延,传输时延,传播时延
- 变化:排队时延

### R17:
- 1000km,1Mbps,100bytes
- 100km,1Mbps,100bytes

### R18:
- 10ms
- d/s
- 无关,只跟距离和光速有关<-这个答案来自中科大的计网课程,官方答案没有解析

### R19:
- 500Kbps
- 64s
- 100kbps,320s

### R20:
- 端系统A把大型文件分成块,给每个块加上一个头部,从而把该文件生成多个分组.每个分组的头部包含目的地(端系统B)的IP地址.
- 分组交换机使用头部中目的地的IP地址决定外向链路
- 询问要走哪条路类似于数据包询问应该在哪个外向链路上转发

### R21:
- 最大的发射速率是500包/秒,最大传输速率为350包/秒.相应的交通强度为500/350=1.43>1.每次实验都会出现丢包.但是由于发射过程中的随机性,所以会出现不同.

### R22:
- 五个通用任务是差错控制,流量控制,分割充足,复用,连接设置.可以,例如:差错控制通常提供在多个层

### R23:
- 
    1. 物理层--在物理介质上加载还原数字信号
    2. 链路层--完成相邻两点之间的传输,可以是可靠的,可以是不可靠的
    3. 网络层--完成主机到主机,端到端的,穿过整网的数据传输,分组传输,包括转发,路由
    4. 运输层--加上网络层的服务,多路复用,可靠性,拥塞控制,网络控制
    5. 应用层--根据传输层提供的服务交换报文,实现应用协议,实现各种应用

### R24:
1. 应用层报文:应用程序希望发送并传递到传输层的数据
2. 传输层报文段:由传输层生成,用传输层报头封装应用层消息
3. 网络层数据报:用网络层报头封装传输层段
4. 链路层帧:用链路层帧头帧尾封装网络层数据报

### R25:
- 网络层,链路层,物理层
- 链路层,物理层
- 应用层,运输层,网络层,链路层,物理层

### P26:
- 病毒是一种需要某种形式的用户交互来感染用户设备的恶意软件
- 蠕虫是一种无须任何明显用户交互就能进入设备的恶意软件

### P27:
- 创建僵尸网络需要攻击者在某些应用程序或系统中找到漏洞(例如,利用应用程序中坑存在的缓冲区溢出漏洞).找到漏洞后,攻击者需要扫描哪些主机是脆弱的,目标基本上是通过利用这些特定的脆弱性来破坏一系列系统.任何属于僵尸网络的系统都可以通过漏洞自动扫描其环境并传播.这些僵尸网络的一个重要特性是,僵尸网络的发起者可以远程控制和向僵尸网络中的所有节点发出命令.
- 攻击者可以向所有节点发出命令,即针对单个节点(例如,僵尸网络中的所有节点都可能被攻击者命令向目标发送TCP SYN消息,这可能导致对目标的TCP SYN洪泛攻击)

### P28:
1. 窃取隐私
2. 伪装数据发送

### P1:
- 协议:
    1. 如果用户卡和口令是对的就运行进行以下操作,如果是错误的,返回提示:用户卡或者口令错误
    2. 自动柜员机发出查询请求,中央电脑返回数据
    3. 自动柜员机发出支取账目请求,中央电脑检查柜员机余额,如果账目请求<余额,返回允许支出.不满足:返回中央柜员机提示:余额不足,请换一台机器
- 图:略
- 分为应用层和传输层,应用层使用传输层传来的信息,传输层基于本地网络至少,假如用户卡或口令正确,每次于应用层的报文开头加上特定信息表示允许访问

### P2:
- 在N*(L/R)时,第一个数据包已经到达目的地,第二个数据包存储在最后一个路由器中,第三个数据包存储在下一个路由器中.等等.当N*(L/R)+L/R,第二个数据包到达目的地,故当N*(L/R)+(P-1)*(L/R)=(N+P-1)*(L/R)时所有数据包都已到达目的地

### P3:
- a.电路交换网,因为该应用包含可预测平滑带宽的长对话需求.因为传输速率是已知的非突发的,因此可以为每个应用会话保留带宽，而不会造成显著浪费。此外，建立和拆除连接的间接费用会在典型应用程序会话的长时间内摊销。
- b.由于每个链路都有足够的带宽来处理所有应用程序的数据速率之和，因此不会出现拥塞。故不需要.

### P4:
- a.16
- b.8
- c.可以,对于A和C之间的连接,我们通过B和D分别设立两个连接,对于B和D之间的连接,我们分别设立A和C两个连接.
故总共为8条连接

### P5:
#### 收费亭相距75公里,汽车以每小时100公里的速度行驶,收费亭每12秒为一辆车服务
- a.经过第一个收费站延迟为12s*10=120s=2min,经45min传播延迟到第二个收费站,经2min,经45min到第三个收费站,经2min.共计96min(类比一个包中十条数据)
- b.94min48s

### P6:
- a.dprop=m/s
- b.dtrans=L/R
- c.端到端时延=传播+传输=m/s+L/R
- d.刚离开主机A
- e.传播时延大于传输时延时,在t=传输时延,第一个bit在路上(时延越小越快)
- f.上题所述,已经到达B
- g.120/(56000)*(2.5*10^8)=536km

### P7:
- 在开始传输前,必须生成包中所有bit,需要:(56*8)/(64*10^3)=7毫秒,传输延迟是:(56*8)/(2*10^6)=224微秒.传播是10ms,故一共是:17.224毫秒

### P8:
- a.3Mbps/150kbps=20
- b.p=0.1
@import "imgs/iShot_2023-04-11_10.39.23.jpg"

### P9:
- a.10000
- b.
@import "imgs/chapter_01_p9.jpg"

### P10:
- L/Ri:数据包传输第i条链路,di/si在第i条上传播,3条链路2个分组交换机->d(proc)*2.所以
@import "imgs/img3.jpg"
- 代入上式:6+6+6+20+16+4+3+3=64ms

### P11:
- 因为是立即传输,所以只传输一次所以算式
$ L/R+di/si $
代入P10数据,formul=46

### P12:
- 27ms
- $ (n*L+(L-x))/R $

### P13:
- 等差数列公式:$ ((N-1)/L)/2R $
- $ (N-1)/2R < LN/R $ 所以下一堆分组到达时队列已空,所以还是 $ ((N-1)/L)/2R $

### P14:
- $ (IL/(R*(1-I)))+ L/R=(L/R)/(1/I) $
- 令x=L/R->总延迟:x/(1-ax)
图像略
### P15:
@import "imgs/img4.jpg"

### P16:
- N=在缓存中的分组加上被传输的分组的平均数(翻译过来果然语义模棱两可)=10+1=11
N=a*(排队时延+传输时延)->11=a*(0.01+1/100)->a=550

### P17:
@import "imgs/img5.jpg"

### P18:(给出例子:从San Diego Super Computer Center和www.poly.edu服务器 )
- 测试三次:71.18ms-0.075ms,71.38ms-0.21ms,71.55ms-0.05ms
- 12个,没有发生变化
- 经过4个ISP,应当被认为是同一个ISP的一部分,在相邻的ISP间的对等接口处出现了最大的时延
- 情况大体一致

### P19.略
### P20.
- min{Rs,Rc,R/M}
### P21.
@import "imgs/img5.jpg"

### P22.
- p=(1-p)^N
- 依上题知,一个分组被成功接收到的概率是p,则,平均传输次数是1/p,因此,重传的平均数是(1/p)-1

### P23:
- 假如瓶颈链路是第一个链路,则数据包B在第一个链路排队等待数据包A的传输,因此就是L/R
- 是一定的,由于前段传输速率快,时延比第二段小.
@import "imgs/img7.jpg"

### P24:
- 计算可知需要37天(注意1byte=8bits).所以选FedEx

### P25:
- $ 20 000km(注意单位)/(2.5*10^8)*2Mbps=160 000bits $
- 160000bits
- 是在链路中同一时刻的能存在的最大bits
- (20 000km)/160 000bits=125meters
- 据第四题知:s/R

### P26:
- 满足:s/R:m=1:1 所以代入知R=12.5bps

### P27:
- 8*10^7
- 8*10^5,因为小于带宽时延积
- 0.25meters

### P28:
- 800 000/2Mbps=400msec,20000km/2.5*10^8=80msec,所以一共是480msec
- 20*(传输时延+传播时延)=2sec
- 拆分文件需要更长时间来传输,因为传输每个数据包都会产生一个传播时延

### P29(引用数据-百度百科:离地表35786km约为36000km):
- 约等于150msec
- 1500 000bits
- 600 000 000 bits

### P30:略

### P31:
- 4s,12s
- 5msec,在第一交换机接收第二分组的时间=在第二交换机接收第一分组的时间=2×5msec =10msec,
- 15msec*(800-1)*5msec=4.01s,将链路全部利用起来,a中某时刻只有一条链路在传播
- d.
    1. 没有消息分割，如果比特错误是不能容忍的，如果有单个比特错误，则必须重新传输整个消息（而不是单个数据包）。
    2. 在没有消息分割的情况下，巨大的数据包（例如包含高清视频）被发送到网
络中。 路由器必须容纳这些巨大的数据包。 较小的数据包必须在巨大的数据包后面排
队，并遭受不公平的延迟。
- e.
    1. 数据包必须按顺序放置在目的地。
    2. 消息分割导致许多较小的数据包。 由于首部大小通常对所有数据包相同，而
不管它们的大小如何，通过消息分割，首部字节的总数更多。

### P32:

###### 注:本标准答案来自官网英文答案(部分自己翻译),某大佬(未备注姓名),中科大习题课